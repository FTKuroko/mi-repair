<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mi_repair.mapper.UserMapper">
    <insert id="register" parameterType="UserRegDTO"  useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (name, password, phone, addr)
        VALUES (#{name}, #{password}, #{phone}, #{addr})
    </insert>
    <select id="login" parameterType="UserLoginDTO" resultType="User">
        SELECT * FROM users
        WHERE name = #{name}
        AND password = #{password}
    </select>
</mapper>
